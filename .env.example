# Copia este archivo a `.env` y completa el valor real.
# El `.env` NO se versiona (está en .gitignore). Nunca commitear el ticket.
#
# Ticket de la API de Mercado Público (api.mercadopublico.cl).
# Se solicita en https://www.chilecompra.cl/api/ — es una credencial de solo
# lectura sobre datos públicos, pero se trata como secreto.
#
# Ticket de PRUEBA público (solo pruebas, no producción):
#   F8537A18-6766-4DEF-9E59-426B4FEE2844
MERCADO_PUBLICO_TICKET=
